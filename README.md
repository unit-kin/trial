# Overview - Simple Shell
This repository contains C functions to build a simple shell simulation

To Compile,
	```gcc -Wall -Werror -Wextra -pendatic -std=gnu89 *.c -o hsh```

To Run,
	```source ./hsh```

Use Ctrl+D to end the program
